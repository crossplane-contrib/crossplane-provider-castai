---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: aioptimizerhostedmodels.castai.upbound.io
spec:
  group: castai.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - castai
    kind: AIOptimizerHostedModel
    listKind: AIOptimizerHostedModelList
    plural: aioptimizerhostedmodels
    singular: aioptimizerhostedmodel
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AIOptimizerHostedModel is the Schema for the AIOptimizerHostedModels
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AIOptimizerHostedModelSpec defines the desired state of AIOptimizerHostedModel
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  clusterId:
                    description: |-
                      (String) CAST AI cluster ID where the model will be deployed.
                      CAST AI cluster ID where the model will be deployed.
                    type: string
                  edgeLocationIds:
                    description: |-
                      (List of String) List of edge location IDs where the model can be deployed.
                      List of edge location IDs where the model can be deployed.
                    items:
                      type: string
                    type: array
                  fallback:
                    description: |-
                      (Block List, Max: 1) Fallback model settings. (see below for nested schema)
                      Fallback model settings.
                    items:
                      properties:
                        enabled:
                          description: (Boolean)
                          type: boolean
                        model:
                          description: (String)
                          type: string
                        providerId:
                          description: (String)
                          type: string
                      type: object
                    type: array
                  hibernation:
                    description: |-
                      (Block List, Max: 1) Automatic hibernation settings. (see below for nested schema)
                      Automatic hibernation settings.
                    items:
                      properties:
                        enabled:
                          description: (Boolean)
                          type: boolean
                        hibernateCondition:
                          description: '(Block List, Min: 1, Max: 1) (see below for
                            nested schema)'
                          items:
                            properties:
                              duration:
                                description: |-
                                  (String) Time period for the condition evaluation.
                                  Time period for the condition evaluation.
                                type: string
                              requestCount:
                                description: |-
                                  (Number) Request count threshold. Value of 0 is treated as not set.
                                  Request count threshold. Value of 0 is treated as not set.
                                type: number
                            type: object
                          type: array
                        resumeCondition:
                          description: '(Block List, Min: 1, Max: 1) (see below for
                            nested schema)'
                          items:
                            properties:
                              duration:
                                description: |-
                                  (String) Time period for the condition evaluation.
                                  Time period for the condition evaluation.
                                type: string
                              requestCount:
                                description: |-
                                  (Number) Request count threshold. Value of 0 is treated as not set.
                                  Request count threshold. Value of 0 is treated as not set.
                                type: number
                            type: object
                          type: array
                      type: object
                    type: array
                  horizontalAutoscaling:
                    description: |-
                      (Block List, Max: 1) Horizontal autoscaling settings. (see below for nested schema)
                      Horizontal autoscaling settings.
                    items:
                      properties:
                        enabled:
                          description: (Boolean)
                          type: boolean
                        maxReplicas:
                          description: (Number)
                          type: number
                        minReplicas:
                          description: (Number)
                          type: number
                        targetMetric:
                          description: (String)
                          type: string
                        targetValue:
                          description: (Number)
                          type: number
                      type: object
                    type: array
                  modelSpecsId:
                    description: |-
                      existing model specs ID for predefined (CastAI-managed) models.
                      ID of the model specs. Can reference a castai_ai_optimizer_model_specs resource or a pre-existing model specs ID for predefined (CastAI-managed) models.
                    type: string
                  nodeTemplateName:
                    description: |-
                      (String) Node template name for model deployment.
                      Node template name for model deployment.
                    type: string
                  port:
                    description: |-
                      (Number) Port on which the model will be exposed.
                      Port on which the model will be exposed.
                    type: number
                  service:
                    description: |-
                      (String) Kubernetes service name for the deployed model.
                      Kubernetes service name for the deployed model.
                    type: string
                  vllmConfig:
                    description: |-
                      (Block List, Max: 1) vLLM configuration for HuggingFace models. (see below for nested schema)
                      vLLM configuration for HuggingFace models.
                    items:
                      properties:
                        huggingFaceTokenSecretRef:
                          description: |-
                            (String, Sensitive) HuggingFace token. Mutually exclusive with secret_name.
                            HuggingFace token. Mutually exclusive with secret_name.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        secretName:
                          description: |-
                            (String) Kubernetes secret name containing the HuggingFace token.
                            Kubernetes secret name containing the HuggingFace token.
                          type: string
                      type: object
                    type: array
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  clusterId:
                    description: |-
                      (String) CAST AI cluster ID where the model will be deployed.
                      CAST AI cluster ID where the model will be deployed.
                    type: string
                  edgeLocationIds:
                    description: |-
                      (List of String) List of edge location IDs where the model can be deployed.
                      List of edge location IDs where the model can be deployed.
                    items:
                      type: string
                    type: array
                  fallback:
                    description: |-
                      (Block List, Max: 1) Fallback model settings. (see below for nested schema)
                      Fallback model settings.
                    items:
                      properties:
                        enabled:
                          description: (Boolean)
                          type: boolean
                        model:
                          description: (String)
                          type: string
                        providerId:
                          description: (String)
                          type: string
                      type: object
                    type: array
                  hibernation:
                    description: |-
                      (Block List, Max: 1) Automatic hibernation settings. (see below for nested schema)
                      Automatic hibernation settings.
                    items:
                      properties:
                        enabled:
                          description: (Boolean)
                          type: boolean
                        hibernateCondition:
                          description: '(Block List, Min: 1, Max: 1) (see below for
                            nested schema)'
                          items:
                            properties:
                              duration:
                                description: |-
                                  (String) Time period for the condition evaluation.
                                  Time period for the condition evaluation.
                                type: string
                              requestCount:
                                description: |-
                                  (Number) Request count threshold. Value of 0 is treated as not set.
                                  Request count threshold. Value of 0 is treated as not set.
                                type: number
                            type: object
                          type: array
                        resumeCondition:
                          description: '(Block List, Min: 1, Max: 1) (see below for
                            nested schema)'
                          items:
                            properties:
                              duration:
                                description: |-
                                  (String) Time period for the condition evaluation.
                                  Time period for the condition evaluation.
                                type: string
                              requestCount:
                                description: |-
                                  (Number) Request count threshold. Value of 0 is treated as not set.
                                  Request count threshold. Value of 0 is treated as not set.
                                type: number
                            type: object
                          type: array
                      type: object
                    type: array
                  horizontalAutoscaling:
                    description: |-
                      (Block List, Max: 1) Horizontal autoscaling settings. (see below for nested schema)
                      Horizontal autoscaling settings.
                    items:
                      properties:
                        enabled:
                          description: (Boolean)
                          type: boolean
                        maxReplicas:
                          description: (Number)
                          type: number
                        minReplicas:
                          description: (Number)
                          type: number
                        targetMetric:
                          description: (String)
                          type: string
                        targetValue:
                          description: (Number)
                          type: number
                      type: object
                    type: array
                  modelSpecsId:
                    description: |-
                      existing model specs ID for predefined (CastAI-managed) models.
                      ID of the model specs. Can reference a castai_ai_optimizer_model_specs resource or a pre-existing model specs ID for predefined (CastAI-managed) models.
                    type: string
                  nodeTemplateName:
                    description: |-
                      (String) Node template name for model deployment.
                      Node template name for model deployment.
                    type: string
                  port:
                    description: |-
                      (Number) Port on which the model will be exposed.
                      Port on which the model will be exposed.
                    type: number
                  service:
                    description: |-
                      (String) Kubernetes service name for the deployed model.
                      Kubernetes service name for the deployed model.
                    type: string
                  vllmConfig:
                    description: |-
                      (Block List, Max: 1) vLLM configuration for HuggingFace models. (see below for nested schema)
                      vLLM configuration for HuggingFace models.
                    items:
                      properties:
                        huggingFaceTokenSecretRef:
                          description: |-
                            (String, Sensitive) HuggingFace token. Mutually exclusive with secret_name.
                            HuggingFace token. Mutually exclusive with secret_name.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        secretName:
                          description: |-
                            (String) Kubernetes secret name containing the HuggingFace token.
                            Kubernetes secret name containing the HuggingFace token.
                          type: string
                      type: object
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.clusterId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.clusterId)
                || (has(self.initProvider) && has(self.initProvider.clusterId))'
            - message: spec.forProvider.modelSpecsId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.modelSpecsId)
                || (has(self.initProvider) && has(self.initProvider.modelSpecsId))'
            - message: spec.forProvider.port is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.port)
                || (has(self.initProvider) && has(self.initProvider.port))'
            - message: spec.forProvider.service is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.service)
                || (has(self.initProvider) && has(self.initProvider.service))'
          status:
            description: AIOptimizerHostedModelStatus defines the observed state of
              AIOptimizerHostedModel.
            properties:
              atProvider:
                properties:
                  cloudProvider:
                    description: |-
                      (String) Cloud provider.
                      Cloud provider.
                    type: string
                  clusterId:
                    description: |-
                      (String) CAST AI cluster ID where the model will be deployed.
                      CAST AI cluster ID where the model will be deployed.
                    type: string
                  currentReplicas:
                    description: |-
                      (Number) Current number of replicas.
                      Current number of replicas.
                    type: number
                  edgeLocationIds:
                    description: |-
                      (List of String) List of edge location IDs where the model can be deployed.
                      List of edge location IDs where the model can be deployed.
                    items:
                      type: string
                    type: array
                  fallback:
                    description: |-
                      (Block List, Max: 1) Fallback model settings. (see below for nested schema)
                      Fallback model settings.
                    items:
                      properties:
                        enabled:
                          description: (Boolean)
                          type: boolean
                        model:
                          description: (String)
                          type: string
                        providerId:
                          description: (String)
                          type: string
                      type: object
                    type: array
                  hibernation:
                    description: |-
                      (Block List, Max: 1) Automatic hibernation settings. (see below for nested schema)
                      Automatic hibernation settings.
                    items:
                      properties:
                        enabled:
                          description: (Boolean)
                          type: boolean
                        hibernateCondition:
                          description: '(Block List, Min: 1, Max: 1) (see below for
                            nested schema)'
                          items:
                            properties:
                              duration:
                                description: |-
                                  (String) Time period for the condition evaluation.
                                  Time period for the condition evaluation.
                                type: string
                              requestCount:
                                description: |-
                                  (Number) Request count threshold. Value of 0 is treated as not set.
                                  Request count threshold. Value of 0 is treated as not set.
                                type: number
                            type: object
                          type: array
                        resumeCondition:
                          description: '(Block List, Min: 1, Max: 1) (see below for
                            nested schema)'
                          items:
                            properties:
                              duration:
                                description: |-
                                  (String) Time period for the condition evaluation.
                                  Time period for the condition evaluation.
                                type: string
                              requestCount:
                                description: |-
                                  (Number) Request count threshold. Value of 0 is treated as not set.
                                  Request count threshold. Value of 0 is treated as not set.
                                type: number
                            type: object
                          type: array
                      type: object
                    type: array
                  horizontalAutoscaling:
                    description: |-
                      (Block List, Max: 1) Horizontal autoscaling settings. (see below for nested schema)
                      Horizontal autoscaling settings.
                    items:
                      properties:
                        enabled:
                          description: (Boolean)
                          type: boolean
                        maxReplicas:
                          description: (Number)
                          type: number
                        minReplicas:
                          description: (Number)
                          type: number
                        targetMetric:
                          description: (String)
                          type: string
                        targetValue:
                          description: (Number)
                          type: number
                      type: object
                    type: array
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  modelSpecsId:
                    description: |-
                      existing model specs ID for predefined (CastAI-managed) models.
                      ID of the model specs. Can reference a castai_ai_optimizer_model_specs resource or a pre-existing model specs ID for predefined (CastAI-managed) models.
                    type: string
                  namespace:
                    description: |-
                      (String) Kubernetes namespace.
                      Kubernetes namespace.
                    type: string
                  nodeTemplateName:
                    description: |-
                      (String) Node template name for model deployment.
                      Node template name for model deployment.
                    type: string
                  port:
                    description: |-
                      (Number) Port on which the model will be exposed.
                      Port on which the model will be exposed.
                    type: number
                  region:
                    description: |-
                      (String) Region the model is deployed in.
                      Region the model is deployed in.
                    type: string
                  service:
                    description: |-
                      (String) Kubernetes service name for the deployed model.
                      Kubernetes service name for the deployed model.
                    type: string
                  status:
                    description: |-
                      (String) Hosted model status.
                      Hosted model status.
                    type: string
                  statusReason:
                    description: |-
                      (String) Reason for the current status.
                      Reason for the current status.
                    type: string
                  vllmConfig:
                    description: |-
                      (Block List, Max: 1) vLLM configuration for HuggingFace models. (see below for nested schema)
                      vLLM configuration for HuggingFace models.
                    items:
                      properties:
                        secretName:
                          description: |-
                            (String) Kubernetes secret name containing the HuggingFace token.
                            Kubernetes secret name containing the HuggingFace token.
                          type: string
                      type: object
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
